# Variáveis de ambiente — Guia do Participante iLab2026
# Copie este arquivo para .env e ajuste os valores

# ── Servidor ──────────────────────────────────────────
# Porta em que o servidor Express escuta
PORT=5000

# Ambiente (development | production)
NODE_ENV=development

# ── Enquete Security Day ───────────────────────────────
# Número máximo de votos por empresa (estoque de vagas)
MAX_VOTES=27

# Número de empresas que cada usuário deve selecionar
MAX_SELECTIONS=8

# Janela de rate limiting em milissegundos (padrão: 60s)
RATE_LIMIT_MS=60000

# Caminho do arquivo JSON que persiste os votos (relativo ao build)
# VOTE_FILE=server/votes.json
